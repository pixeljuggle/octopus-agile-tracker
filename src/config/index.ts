export const DEVELOPMENT_ENVIRONMENT = Boolean(!process.env.NODE_ENV || process.env.NODE_ENV === 'development');
export const USE_API_KEY = false;
export const CORS_PROXY = 'https://cors.zeep.workers.dev/';
